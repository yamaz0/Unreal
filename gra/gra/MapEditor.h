#pragma once
#include <SFML/Graphics.hpp>
#include "Text.h"

class MapEditor
{/*
 To chce zrobic juz samemu, nie w ramach projektu do oddania.
	sf::Mouse mouse;
	sf::Keyboard keyboard;
	Text komunikat;
	sf::View view;
 */

public:
	MapEditor(sf::RenderWindow &);
	~MapEditor();
};

